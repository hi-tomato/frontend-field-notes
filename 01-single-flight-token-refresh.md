# 동시 401이 세션을 끊던 문제

## 상황

지갑 홈 화면은 진입과 동시에 잔액·시세·알림 뱃지·리워드를 병렬로 조회한다.
액세스 토큰이 만료된 상태로 진입하면 요청 4~5개가 한꺼번에 401을 받는다.

각 요청의 인터셉터는 저마다 토큰 갱신을 시도했다. 문제는 서버가 사용자당
refresh 토큰을 **1개만 유지하고, 갱신 즉시 회전**시킨다는 것. 첫 번째 갱신이
성공하는 순간 두 번째 갱신이 들고 간 토큰은 이미 폐기된 값이다. 서버는 401로
답하고, 클라이언트는 이를 세션 만료로 판단해 강제 로그아웃했다.

**토큰이 만료된 상태에서 앱을 켜기만 해도 로그아웃되는** 상황이었다.

## 왜 불린 플래그로는 안 되는가

가장 먼저 떠오르는 방어는 플래그다.

```ts
let isRefreshing = false;

async function refresh() {
  if (isRefreshing) return;   // (1) 검사
  isRefreshing = true;        // (2) 대입
  await requestNewToken();    // (3) 갱신
  isRefreshing = false;
}
```

JS는 싱글 스레드지만, (1)과 (2) 사이가 원자적이라는 보장은 **같은 동기 블록 안에
있을 때만** 성립한다. 인터셉터는 비동기 컨텍스트에서 호출되고, 검사와 대입 사이에
다른 요청의 마이크로태스크가 끼어들 수 있는 구조라면 두 요청 모두 (1)을 통과한다.
실제로 재현됐다.

요청을 큐에 쌓는 방식도 검토했지만, 갱신이 **예외로 끝났을 때 큐를 비우는 책임**이
남는다. 비우는 것을 잊은 코드 경로가 하나라도 있으면 대기자들이 영원히 매달린다.

## 선택: 갱신 Promise 자체를 공유한다

```ts
let refreshPromise: Promise<string> | null = null;

function refreshToken(): Promise<string> {
  if (refreshPromise) return refreshPromise;  // 검사와 반환이 동기 한 줄

  refreshPromise = performRefresh().finally(() => {
    refreshPromise = null;
  });
  return refreshPromise;
}
```

- 검사와 반환 사이에 `await`가 없다 — 끼어들 틈 자체가 없다
- 실패하면 대기자 전원이 **같은 Promise로 함께 reject**된다 — 큐를 비우는
  것을 잊을 코드가 존재하지 않는다

## 함께 정리한 것

1. **선제 갱신** — 만료 60초 전이면 요청 인터셉터에서 미리 갱신한다. 이미 갱신
   중이면 그 결과를 기다렸다 새 토큰으로 나간다.
2. **실패의 분류** — 갱신 실패를 인증 실패(401, refresh 만료)와 네트워크 오류로
   구분했다. 네트워크 오류는 세션을 유지하고 요청만 실패시킨다. 지하철 같은
   약전파 환경에서 발생하던 불필요한 로그아웃이 사라졌다.
3. **재귀 회피의 대가** — 갱신 요청은 인터셉터가 없는 raw 클라이언트로 보내는데,
   그러면 기본 timeout도 함께 사라진다. 명시하지 않으면 무한 대기가 된다.

## 배운 것

동시성 문제는 "락을 건다"가 아니라 **"검사와 점유 사이의 틈을 없앤다"**로 접근해야
한다. 그리고 정리(cleanup)가 필요한 설계보다, **정리할 것이 없는 설계**가 낫다 —
공유 Promise는 후자다.
