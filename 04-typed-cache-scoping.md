# 지갑을 바꿔도 이전 잔액이 보이던 문제

## 상황

지갑 연결을 해제하고 다른 지갑으로 다시 연결했는데, 홈 화면에 **이전 지갑의
잔액이 그대로** 표시됐다. 자산 금액이 걸린 화면에서 이보다 나쁜 표시는 없다 —
틀린 숫자보다 "그럴듯하게 틀린" 숫자가 더 위험하다.

## 원인

잔액 조회의 React Query 키가 이렇게 생겼었다.

```ts
queryKey: ["blockchain", "price"]
```

응답은 **인증된 사용자의 지갑** 내용인데, 키에는 지갑을 식별하는 값이 없다.
지갑이 바뀌어도 React Query 입장에서는 같은 키이므로 캐시를 재사용한다.
staleTime 안에 재연결하면 refetch조차 일어나지 않는다.

## 시도했다가 되돌린 것: 전역 캐시 초기화

가장 빠른 수습은 지갑 해제 시 `queryClient.clear()`였다. 실제로 그렇게 고쳤다가
**되돌렸다.** 지갑과 무관한 공지·FAQ·약관 캐시까지 날아가서, 전환 후 모든
화면이 다시 로딩됐기 때문이다. 전역 초기화는 문제의 크기에 비해 부작용이
크고, 무엇보다 **원인(키 스코프 누락)을 그대로 둔 채 증상만 덮는다.**

## 선택: 키에 넣고, 넣는 것을 강제한다

키에 주소를 추가하면 지금 버그는 사라진다.

```ts
queryKey: ["blockchain", "price", address]
```

하지만 이것으로 끝내면, 다음에 잔액 관련 쿼리를 추가하는 사람이 스코프를
빠뜨렸을 때 같은 버그가 그대로 재발한다. 문서와 리뷰에 기대는 방어다.

그래서 키 팩토리에서 **주소를 필수 인자로 선언**했다.

```ts
export const queryKeys = {
  blockchain: {
    all: ["blockchain"] as const,
    // 응답은 인증된 사용자의 지갑 내용 — 캐시는 반드시 주소로 스코프한다.
    // 파라미터를 필수로 두면, 스코프를 빠뜨린 새 호출부는 컴파일되지 않는다.
    price: (address: string) =>
      [...queryKeys.blockchain.all, "price", address] as const,
  },
};
```

인자를 빼먹은 호출은 런타임이 아니라 **컴파일 단계에서 실패**한다. 같은
실수가 들어올 경로 자체를 닫은 것이다.

한 가지 더 — 왜 필수인지의 이유를 주석으로 남겼다. 이유가 없으면 다음 사람이
"선택 인자로 바꿔도 되지 않나" 하고 제약을 되돌릴 수 있는 종류의 코드이기
때문이다. 제약은 이유와 함께 있을 때만 살아남는다.

## 배운 것

캐시 키는 "무엇을 조회하는가"가 아니라 **"응답이 무엇에 의존하는가"**를 담아야
한다. 그리고 규칙은 문서보다 타입으로 강제하는 편이 싸다 — 문서는 읽지 않은
사람에게 무력하지만, 컴파일 에러는 모두에게 평등하다.
