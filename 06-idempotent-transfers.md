# 같은 송금이 두 번 나갈 수 있던 문제

## 상황

토큰 전송은 두 갈래다. 온체인 전송은 서명된 트랜잭션을 브로드캐스트하고,
리워드 출금은 서버 API를 호출한다. 양쪽 모두에 재시도 로직이 있었다 —
모바일 네트워크는 자주 끊기니까.

감사 과정에서 두 경로 모두에서 같은 결함을 찾았다.
**재시도가 같은 돈을 두 번 보낼 수 있었다.**

## 문제의 구조

온체인 쪽: 브로드캐스트 요청이 나간 뒤 **응답을 받기 전에** 네트워크가 끊기면,
클라이언트는 실패로 판단하고 폴백 경로로 재전송한다. 그러나 첫 요청은 이미
노드에 도착했을 수 있다. 두 트랜잭션이 모두 체결되면 이중 송금이다.

서버 출금 쪽: 요청에 멱등키를 보내고는 있었는데, **재시도할 때마다 키를 새로
발급**하고 있었다. 서버 입장에서는 매번 처음 보는 요청이다. 멱등키가 있는데
멱등성이 없었다.

## 판단: 위험한 것은 "실패한 재시도"가 아니다

정리하고 나니 원칙이 하나로 모였다.

> 위험한 것은 실패가 확인된 재시도가 아니라,
> **전송됐는지 불확실한 상태의 재시도**다.

- 요청이 나가기 **전**에 실패했다 → 재시도 안전
- 요청이 나간 **후** 결과를 모른다 → 재시도 금지, 확인이 먼저

이 기준으로 양쪽을 고쳤다.

**온체인**: 브로드캐스트 이후에는 성공 여부를 모르더라도 재전송을 금지했다.
최악의 경우가 "한 번 더 확인"이지 "두 번 전송"이 아니게 된다. 안전 방향이
확실한 쪽으로 실패하게 만든 것이다.

**서버 출금**: 멱등키를 재시도 단위가 아니라 **출금 의도 단위**로 고정했다.
사용자가 "이 금액을 보낸다"고 확정한 시점에 키를 1회 발급하고, 이후의 모든
재시도가 같은 키를 재사용한다. 중복 판정은 서버의 일이 되고, 클라이언트는
마음 놓고 재시도할 수 있다.

```ts
// 재시도 루프 밖에서, 의도가 확정될 때 1회 발급
const idempotencyKey = crypto.randomUUID();

async function withdraw(req: WithdrawRequest) {
  return retryable(() =>
    api.post("/withdraw", req, {
      headers: { "Idempotency-Key": idempotencyKey },  // 재시도에도 불변
    })
  );
}
```

키 발급 위치가 전부다 — 루프 안이면 장식이고, 루프 밖이면 멱등성이다.

## 검증

"재시도해도 한 번만 나간다"는 명제를 단위 테스트로 고정했다. 네트워크 단절
시점을 브로드캐스트 전/후로 나눠 각각 재시도 허용/차단을 검증하고, 출금
재시도 시 멱등키가 변하지 않는지를 확인한다. 이 테스트가 있어야 나중에 재시도
로직을 만지는 사람이 안전 불변식을 모르고 깨는 것을 막을 수 있다.

## 배운 것

멱등성은 서버 기능이 아니라 **계약**이다 — 클라이언트가 키를 올바른 단위로
발급하지 않으면 서버 구현은 무력하다. 그리고 분산 환경의 재시도 설계는
"성공/실패"의 2분법이 아니라 **"모름"을 포함한 3분법**으로 시작해야 한다.
사고는 언제나 "모름"을 "실패"로 취급하는 곳에서 난다.
