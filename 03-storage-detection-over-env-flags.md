# 업데이트하면 지갑이 사라지던 문제

## 상황

비수탁 지갑 앱에서 니모닉은 사용자 기기에만 저장된다. 저장 포맷은 두 세대가
공존했다 — 구형(레거시)과 신형(암호화 vault). 어느 포맷으로 읽을지는 빌드에
주입되는 **환경 플래그**로 판별하고 있었다.

앱 업데이트(또는 OTA 배포) 후 일부 사용자의 지갑이 "없음"으로 판정됐다.
니모닉을 다시 입력해야 했고, 백업이 없는 사용자에게는 자산 접근성 문제가 된다.

## 원인

환경 플래그는 **배포마다 바뀔 수 있는 값**이다. 기기에는 구형 포맷 데이터가
있는데 새 빌드의 플래그는 신형을 가리키면, 코드는 신형 위치에서 데이터를 찾고
— 없으니 — 지갑이 없다고 판정한다. 멀쩡한 데이터가 기기에 있는데도.

판별의 진실 원천이 잘못된 곳에 있었다. "지금 빌드가 어떤 모드인가"(환경)는
"이 기기에 무엇이 저장돼 있는가"(사실)를 대변하지 못한다.

## 선택: 데이터가 진실이다

저장된 데이터 자체를 검사해 포맷을 판별하는 함수를 만들고, 호출부의 환경
플래그 분기를 금지했다.

```ts
type StorageMode = "vault" | "legacy" | "none";

async function detectWalletStorage(): Promise<StorageMode> {
  if (await hasVaultPayload()) return "vault";    // 신형 데이터 존재?
  if (await hasLegacyPayload()) return "legacy";  // 구형 데이터 존재?
  return "none";
}
```

호출부마다 플래그 분기를 고치는 대안도 있었지만 제외했다 — 새 호출부가
추가될 때마다 같은 결함이 재발할 수 있는 구조이기 때문이다. 판별을 한 곳으로
모으고, 그곳의 기준을 데이터로 바꾸는 것이 재발 경로를 닫는 방법이었다.

## 함께 해결한 것

- **iOS 재설치 잔존** — iOS는 앱을 지워도 Keychain 데이터가 남는다. 재설치
  감지 마커를 함께 두지 않으면 이전 설치의 암호화 데이터와 새 세션 상태가
  어긋나 로그인이 무한 루프에 빠질 수 있다. 마커를 영구 저장소로 옮겨 해결했다.
- **오류의 분리** — "vault 없음/손상"을 "틀린 PIN"과 다른 오류로 분리했다.
  전자를 후자로 취급하면 사용자는 맞는 PIN을 넣고도 잠금 카운트가 쌓인다.
  전자는 복구 안내로, 후자만 잠금 정책으로 이어지게 했다.

## 결과

업데이트·재설치 후에도 지갑이 유지된다. 구버전 → 신버전 업그레이드 시나리오를
시뮬레이터에서 검증하고, 판별 로직을 단위 테스트로 고정했다.

## 배운 것

상태 판별의 진실 원천은 **배포와 함께 변하는 값에 두면 안 된다.** 환경 변수는
"이 빌드가 무엇을 원하는가"를 말할 뿐, "기기에 무엇이 있는가"는 데이터만이
말할 수 있다. 마이그레이션 코드에서 이 둘을 혼동하면, 가장 오래된 사용자가
가장 먼저 다친다.
